// ARQUIVO DESATIVADO: voltamos a usar um banco único e compartilhado (ver
// prisma/schema.prisma) — não existe mais um banco de controle separado.
// Use `@/lib/prisma` (client `prisma`) diretamente, ou `@/lib/tenant-prisma`
// / `@/lib/session-tenant` para acesso já filtrado por empresa.
export {};
